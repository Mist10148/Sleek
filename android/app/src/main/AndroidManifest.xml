<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required to fetch and download from YouTube. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Legacy write access for saving to public dirs on Android 9 and below.
         Android 10+ uses scoped storage / the system directory picker. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- Needed when users pick a public directory on Android 10–12 (API 29–32).
         On Android 13+ this is replaced by granular media permissions. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- Lets the in-app player detect & play music already on the device
         (Android 13+ granular media permission for audio). -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <!-- on_audio_query's Android backend checks for BOTH READ_MEDIA_AUDIO and
         READ_MEDIA_IMAGES on Android 13+ (it also surfaces embedded artwork via
         MediaStore.Images) before honouring any query. Without this declared
         and granted, its internal permission check fails even though our own
         `Permission.audio` check reports success — and its `querySongs` handler
         then replies to the platform-channel result twice (once with an error,
         once with the query results, since it's missing a `return` after the
         first), crashing the whole app with "Reply already submitted". -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:label="Sleek"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
